#include "lists.h"
/**
 * is_palindrome - check if a linked list in palindrome
 * @head: head of linked list
 * Return: int 0 or 1
*/
int is_palindrome(listint_s **head)
{
if head == NULL
{
return (1);
}
}
